# UJ — Universal JSON

A tiny JSON wrapper with optional standardization and diffing.

UJ helps you:
- wrap payloads into a consistent structure
- optionally standardize messy JSON-compatible data
- compute precise changes between two states

## Install

```bash
npm install uj
